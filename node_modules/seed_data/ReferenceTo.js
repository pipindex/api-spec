/*
    Used in the seed data to represent a reference to some other object, to be resolved when builtSeedData.js builds
    everything.
*/

module.exports = function ReferenceTo(referencedObjectName) {
    this.referencedObjectName = referencedObjectName;
};