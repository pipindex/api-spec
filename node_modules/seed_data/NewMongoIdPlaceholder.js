/*
    Used in the seed data to represent that we need to generate a new MongoID and insert it.

    We use this on embedded objects that need IDs; top-level objects automatically get given IDs by builtSeedData.js.

    The 'name' field is optional and will be looked up from idsToBakeIn to see if there's a hard-coded ID we should use
    for this object.
*/

module.exports = function NewMongoIdPlaceholder(name) {
    this.name = name;
};